# MLP 단독 모델은 순수 torch.nn + scikit-learn(OrdinalEncoder/StandardScaler)만 사용합니다.
# torch==2.7.1+cu128, pandas, numpy, scikit-learn==1.8.0은 평가 서버에 기본 설치되어 있으므로
# 여기 다시 명시하지 않습니다(버전 충돌로 인한 설치 오류를 피하기 위함).
# 추가로 설치할 패키지가 없습니다.
